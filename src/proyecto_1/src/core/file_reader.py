class FileReader:
    """
    Handles file operations and initial validation
    
    Methods:
    - read_file(path: str) -> str
      Basic file reading and validation
    
    - validate_file_type(path: str) -> bool
      Checks for .py extension
    
    - check_file_access(path: str) -> bool
      Verifies read permissions
    """