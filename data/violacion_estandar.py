# Asignaciones múltiples con ;
# a = 1; b = 0; c = 0

# Operador Ternario Anidado
# x = 10 if 10 > 5 else 5 if 5 > 3 else 3

# Función lambda
# f = lambda x: x + 1

# List Comprehension anidado
# l = [x for x in [y for y in range(10)] if x % 2 == 0]

# Set Comprehension anidado
# s = {x for x in {y for y in range(10)} if x % 2 == 0}

# Dict Comprehension anidado
# d = {x: x for x in {y: y for y in range(10)} if x % 2 == 0}

# Generador anidado
# g = (x for x in (y for y in range(10)) if x % 2 == 0)